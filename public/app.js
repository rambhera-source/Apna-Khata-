const form=document.querySelector('#login-form');const password=document.querySelector('#password');const error=document.querySelector('#error');document.querySelector('#toggle-password').addEventListener('click',e=>{const hidden=password.type==='password';password.type=hidden?'text':'password';e.currentTarget.textContent=hidden?'Hide':'Show';e.currentTarget.setAttribute('aria-label',hidden?'Hide password':'Show password')});form.addEventListener('submit',e=>{e.preventDefault();const username=form.username.value.trim();if(!username||!password.value){error.textContent='Please enter both username and password.';(!username?form.username:password).focus();return}error.textContent='This web preview is ready. Connect your account service to enable sign-in.'});document.querySelector('#forgot').addEventListener('click',e=>{e.preventDefault();error.textContent='Please contact your company administrator to reset access.'});

